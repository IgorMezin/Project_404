@page
@using KonohagoWebApp.Helpers
@using Microsoft.AspNetCore.Http
@using KonohagoWebApp.Models
@model KonohagoWebApp.Pages.ProfileModel
@{
    var user = Model.User;
    bool IsOwner = user.Id == HttpContext.Session.Get<User>("Current_user").Id;
}
<head>
    <link rel="stylesheet" href="/css/profile.css">
</head>
<body>
<div class="container-fluid main" style="height:100vh;padding-left:0px;">

    <div class="d-block d-md-none menu">

        <div class="bar"></div>

    </div>

    <div class="row align-items-center" style="height:70%">
        <div class="col-md-12">

            <div class="container content clear-fix">

                <div class="row" style="height:100%">

                    <div class="col-md-3">

                        <!--                        <div href=# class="d-inline"><img src="https://image.flaticon.com/icons/svg/236/236831.svg" width=130px style="margin:0;"><br>-->
                        <!--  <p class="pl-2 mt-2"><a href="#" class="btn" style="color:#8f9096;font-weight:600">Edit Picture</a></p></div>-->
                        
                        <div href=# class="d-inline"><img src="@user.ImagePath" width=130px></div>
                        <form method="post">
                            @if (IsOwner)
                            {
                                <p class="col-md-2">
                                    <input asp-for="Avatar" class="btn" type="file" style="color:#8f9096;font-weight:600;" placeholder="Edit picture">
                                </p>
                            }
                        </form>
                        


                    </div>

                    <div class="col-md-9">

                        <div class="container">

                            <form>

                                <div class="form-group">

                                    <label for=fullName>@user.Nickname</label>
                                    @if (IsOwner)
                                    {
                                    <input type="text" class="form-control" id="fullName">
                                    }

                                </div>
                                <div class="form-group">

                                    <label for=email>@user.Email</label>
                                    @if (IsOwner)
                                    {
                                    <input type="email" class="form-control" id="email">
                                    }
                                </div>
                                @if (IsOwner)
                                {
                                <div class="row mt-5">

                                    <div class="col">

                                        <button type="button" class="btn btn-primary btn-block">Сохранить</button>

                                    </div>

                                    <div class="col">

                                        <button type="button" class="btn btn-default btn-block">Назад</button>

                                    </div>

                                </div>
                                }
                            </form>

                        </div>

                    </div>

                </div>

            </div>

        </div>

    </div>

</div>
</body>